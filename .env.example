# Required only for `npm run discover` (the LLM-driven discovery run).
# Replay, tests, the mock app and the operator console run without it.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides
# DISCOVERY_MODEL=claude-opus-5
# COMPUTER_USE_APP_PORT=4310
